TigerGraph Solution Kit installer

tgSolutionPack - this package contains scripts and data to be used to populate any
   tigergraph instance with on of the popular starter kit demos. Currently the kit
includes these use cases:

    - Airline data
    - Entity Resolution (MDM)
    - Fraud Detection - TBD
    - LDBC benchmark Data
    - TPC-DS Benchmark - TBD
    - Synthea Health Care data
    - Flight Delays - public transportation dataset - TBD
    - Customer 360 - TBD

The package can be installed on any instance containing a tigergraph application.
The install process is simple:

1. Copy (scp for secure copy) the tgSolutionPack.tar.gz archieve file to the target machine. if you are running Docker locally:

         scp -P 14022 tgSolutionPack.tar.gz tigergraph@localhost:~/

            will transfer the file to the root of the tigergraph user home

2. ssh to the container

   ssh -p 14022 tigergraph@localhost

3. uncompress the archive:

	     tar -xzvf tgSolutionPack.tar.gz

4. change directory into the tgSolution pack folder:

      cd tgSolutionPack

5. run the install script and following the instructions
	
      ./runDPInstall.sh

6. Select one of the solution packs to install

   1 - Entity Resolution(MDM)
   2 - Fraud Detection
   3 - LDBC Benchmark
   4 - TPC-DS Benchmark
   5 - Synthea HealthCare
   6 - Flight Delays
   7 - Customer360

7. The script wil create the objects, loading job and begin to load data.

   NOTE: As with most gsql utilities, all existing objects are first removed

8. Go to the Studio UI to see progress 

      - for the local Docker container: http://localhost:14240


